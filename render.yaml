services:
  - type: web
    name: milana-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt || pip install fastapi uvicorn pydantic"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    healthCheckPath: /health
