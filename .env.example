# Copy to .env — NEVER commit .env

AKSI_ADMIN_TOKEN=aksi-admin-dev
AKSI_SQLITE=aksi_phase1.db
AKSI_DID=did:aksi:ed25519:sovereign-2026
RESONANCE_SEED=AKSI_DIMAX_v3_2026

# Cloud LLM (optional — pick one or both; priority: xAI → OpenAI → Ollama)
# XAI_API_KEY=
# XAI_MODEL=grok-3
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Local LLM
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=mistral

# Optional
# GITHUB_TOKEN=
# DATABASE_URL=postgresql://aksi:aksi@localhost/aksi
